[Unit]
Description=OpenClaw Scheduler Heartbeat - authenticated POST /api/scheduler/tick without a Mission Control browser tab
After=network.target

[Service]
Type=oneshot
ExecStart=${NODE_BIN} ${OPENCLAW_WORKSPACE}/bin/scheduler-heartbeat.mjs
