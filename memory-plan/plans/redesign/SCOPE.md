# SCOPE — redesign plan

**Status:** idle
**Goal:** At v4.3 (Block 4: 4.1–4.3 closed; next 4.4). No human driver active. Tick chain disabled — awaiting operator decision on re-enable mode. Viewer observability indicators (tree-dirty, needs-you, BLOCKED-on-stall) landed + verified this session.
**Set at:** 2026-05-31
**Expires:** no-expiry

```files
```

## How this file works

- **Status:** must be `active` for the hook to allow edits to listed files.
- **Expires:** ISO-8601 UTC. Past `Expires` -> blocked. `no-expiry` disables the check.
- **`files` block:** one repo-relative path per line; exact or shell-glob; `#` comments.
- **Override:** `**Override:** true` bypasses the hook (operator emergency escape).
