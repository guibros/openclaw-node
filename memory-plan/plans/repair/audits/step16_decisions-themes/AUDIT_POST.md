# AUDIT_POST — Step 2.9: decisions/ + themes/ surfaces + duplicate aliasing (closes Block 2)

(§0: Block 2, step 11/11, 17/48. Operator shapes confirmed via AskUserQuestion: per-decision notes, per-theme hubs, ALIAS for duplicates — both rows kept, deviating from the merge recommendation; respected.)

## Files-vs-plan ledger

| Planned | Actual | Notes |
|---|---|---|
| `lib/obsidian-decision-notes.mjs` (new) | ✓ | One dated note per high-salience decision (salience ≥0.4, top-30): rationale, confidence, piped-slug concept links, session-note link. Deterministic, write-if-changed, atomic, collision-safe filenames. |
| `lib/obsidian-theme-notes.mjs` (new) | ✓ | One hub per ≥threshold theme: alias, hierarchy, member concepts via extraction-batch approximation (schema gap captured to OUT_OF_SCOPE), honest no-membership line otherwise. |
| `lib/obsidian-summarizer.mjs` | ✓ | Collision-aliasing per the operator's alias decision: alias map built from ALL above-threshold candidates (durable under targeted regens); first-holder owns colliding slug, carries every name; in-run skip kills ping-pong in the local writer too. |
| `lib/obsidian-session-notes.mjs` | ✓ | Mid-step find: this generator still emitted dangling links (34 of them landed during last night's induced flushes) — link-only-existing filter added (note exists → `[[slug]]`, else plain text). |
| `lib/pre-compression-flush.mjs` | ✓ | Both generators wired into the synthesis chain (non-fatal, artifacts reported). |
| `lib/obsidian-link-checker.mjs` + `bin/vault-check.mjs` | ✓ | Coverage report + CLI extended with decision/theme coverage. |
| Tests | ✓ | New file (6 cases: builders, generators, idempotency) + collision-alias case + session-notes filter case + fixture updates. Suite **1521/1521**. |

## Design refinement mid-step
First live run surfaced that slug-existence filtering isn't sufficient when a legacy note lacks its alias — switched decision/theme links to **piped slug form** (`[[slug|Display Name]]`): basename resolution is unconditional in Obsidian, no alias dependency. The 12 residual slug-resolvables went to zero.

## Verification (Phase 5 — the Proof)

- **Surfaces live:** 21 decision notes + 6 theme hubs generated by the deployed libs; regeneration idempotent (themes: 0 changed / 6 unchanged on rerun).
- **Alias resolution:** `concepts/openclaw.md` carries `aliases: ["OpenClaw", "openclaw"]` — both names resolve to one referential identity; both DB rows intact per the operator's decision.
- **Checker:** **867/867 wikilinks resolved (100%), 0 dangling, 0 slug-resolvable.**
- **Coverage report extended:** concepts 69/69, decisions 30/30, themes 6/6 — all 100%.
- **Flush wiring:** exercised by the suite (runFlush integration runs both generators); daemon restarted onto the wired code (PID 40668).

## Macro Re-Orient (Block 2 close, WORKFLOW §7.2)

- **North star:** D7's transparent referential vault is delivered end to end — four surfaces (concepts/sessions/decisions/themes) generated, 100% coverage on all measured dimensions, 100% link resolution, integrity measured on every flush and surfaced in mission-control. The operator's headline for this plan is met.
- **Registry probes:** vault 106 notes / 867 links / 0 dangling; state.db 1073+ entities (69 above threshold), 341 decisions (30 noted), 652 themes (6 hubs); daemon v2.11-era code live.
- **Block 3 re-survey:** steps 3.1–3.4 remain atomic and correctly ordered; D8 audit-first unchanged; new candidates for 3.4's defined-at: theme-linkage schema gap, 50KB floor, extractJsonFromText fast path (R42) — all captured.
- **Drift check:** one process deviation this block (2.10+2.11 shared a commit — documented); a second active scope (`plans/protocol/`) appeared in the hook union mid-block — NOT this plan's; flagged to the operator (one-scope-at-a-time discipline).
- **OUT_OF_SCOPE balance:** 6 captures await triage (memory-maintenance bootstrap exit, entity-duplication canonicalization [partially served by aliasing], promoter-post-D7, 50KB floor, theme linkage, protocol-scope observation pending operator).
