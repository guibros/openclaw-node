Documentation drift audit: MULTI_NODE_DEPLOY.md Part 1 vs installer reality
docs/MULTI_NODE_DEPLOY.md Part 1 predates the current installer. Audit it against what install.sh actually does today (rendered configs, NATS single-node default vs the R=3 cluster cutover, unit placement and naming, post-install steps) and rewrite Part 1 so an operator on a fresh machine following it verbatim would succeed. Cite install.sh line ranges for every corrected claim. Artifact: a short was/now drift table followed by the rewritten Part 1. Read-only analysis: work only inside your worktree checkout and modify no file — the rewritten Part 1 is delivered inside your response, not written to disk. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
