Documentation drift audit: MULTI_NODE_DEPLOY.md Part 1 vs installer reality
docs/MULTI_NODE_DEPLOY.md Part 1 predates the current installer. Audit it against what install.sh actually does today (rendered configs, NATS single-node default vs the R=3 cluster cutover, unit placement and naming, post-install steps). Artifact: a was/now drift table with install.sh line ranges cited for every corrected claim, followed by a patch-ready quickstart of 8-12 numbered commands that would replace Part 1's flow. Do NOT claim the quickstart succeeds on a fresh machine — that requires a live install this task cannot observe; mark any step whose behavior you cannot verify from the source as UNVERIFIED. 500-700 words total. Read-only analysis: work only inside your worktree checkout and modify no file — the drift table and quickstart are delivered inside your response, not written to disk. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
