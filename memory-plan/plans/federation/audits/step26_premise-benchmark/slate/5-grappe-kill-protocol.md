Test protocol design: literal grappe-member kill
Design an executable protocol satisfying federation 6.3's outstanding requirement: kill one registered, heartbeat-producing grappe member during an active circling session and observe the resulting fed.* watcher transition. Establish the registry, member-health, session, role, phase, and PID preconditions. Specify the signal and timing, expected heartbeat-freshness delay, watcher/KV/session evidence, deadline-sweep behavior, abort criteria, and cleanup. Determine from the implementation whether the session should advance, degrade, or abort for the selected role and phase; do not assume survival. Include alternate expected outcomes that would expose a defect rather than reinterpret them as success. Read-only analysis against your worktree checkout only; modify no file; 500-700 words. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
