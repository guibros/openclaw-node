Analysis and fix proposal: strict event verification node-id mismatch
The strict event-verification path is reported to read `event.node_id` while signed envelopes carry `signer_node_id` — an always-fails verification flagged by the July reviewers. Locate the exact mismatch in this repository (start from lib/event-schemas.mjs, lib/broadcast-emitter.mjs, lib/broadcast-acceptor.mjs, lib/node-identity.mjs), reconstruct how a signed envelope actually flows from producer to verifier, and state precisely under which conditions strict verification fails today. Then propose the minimal fix (field rename, dual-read, or envelope change) and its compatibility implications for already-stored events. Artifact: ~400 words — findings with file:line citations, then the recommended fix and why it is minimal. Read-only analysis: work only inside your worktree checkout and modify no file. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
