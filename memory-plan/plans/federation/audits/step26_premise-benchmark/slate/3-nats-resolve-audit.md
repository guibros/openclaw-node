Correctness audit: lib/nats-resolve.js resolution chain
lib/nats-resolve.js resolves the NATS URL and auth token for every client in the stack. Audit the implementation for correctness and edge cases: env-file parsing (quoting, comments, repeated keys, CRLF), precedence order, loopback-first behavior, cluster URL lists, empty-vs-missing token semantics, and behavior when the env file is unreadable. Classify each finding by severity and propose minimal fixes. Artifact: ~400 words — a findings table with file:line citations plus fix proposals. Read-only analysis: work only inside your worktree checkout and modify no file. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
