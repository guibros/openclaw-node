Watcher contract audit: implementation vs NODE_WATCH_SPEC
Compare lib/node-watch.mjs against the locked element list and signals in docs/NODE_WATCH_SPEC.md. Follow delegated probes into lib/node-acceptance-probes.mjs only where node-watch explicitly reuses them. Identify any element that is missing, asserts less than its documented signal, confuses loaded with running, reads stale evidence as fresh, or returns UNKNOWN where an existing signal could provide an observation. Do not assume gaps exist; "none" is valid when demonstrated. Artifact: a gap table followed by the three highest-value corrections, or the three strongest verified contracts if fewer than three gaps exist. Read-only analysis against your worktree checkout only; modify no file; 500-700 words. The artifact must be the document alone — no verification preamble, no execution commentary; your response text IS the deliverable.
